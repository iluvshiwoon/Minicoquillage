#include "../../Minicoquillage.h"
#include "format.h"
#include <stdlib.h>
#include <string.h>
#include <stdio.h>


// t_format_cli **command_line(t_double_link_list **list_o)
// {

// }




// void	*my_realloc(void *ptr, size_t new_size)
// {
// 	if (new_size == 0)
// 	{
// 		free(ptr);
// 		return NULL;
// 	}
// 	if (!ptr)
// 		return malloc(new_size);
// 	void *new_ptr = malloc(new_size);
// 	if (!new_ptr)
// 		return NULL;
// 	ft_memcpy(new_ptr, ptr, new_size - 1);
// 	free(ptr);
// 	return new_ptr;
// }
